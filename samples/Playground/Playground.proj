<Project>
  <PropertyGroup>
    <A>true</A>
  </PropertyGroup>

  <Target Name="Build">
    <PropertyGroup Condition="'$(A)' == 'true'">
      <B>yes</B>
      <C Condition="'$(B)' == 'yes'">ok</C>
    </PropertyGroup>

    <Message Importance="High" Text="C=$(C)" />
  </Target>
</Project>
