<Project>
  <PropertyGroup>
    <Configuration>Release</Configuration>
    <BuildDependsOn>Prepare;CollectSources</BuildDependsOn>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="Program.cs" />
  </ItemGroup>

  <Target Name="Prepare">
    <PropertyGroup>
      <Configuration>Debug</Configuration>
    </PropertyGroup>
  </Target>

  <Target Name="CollectSources">
    <ItemGroup>
      <Compile Include="Generated.cs" />
    </ItemGroup>
  </Target>

  <Target Name="Build"
          DependsOnTargets="$(BuildDependsOn)">
    <ToyCompile Sources="@(Compile)"
                Configuration="$(Configuration)">
      <Output TaskParameter="Assembly"
              PropertyName="AssemblyPath" />
    </ToyCompile>
  </Target>

  <Target Name="AfterBuild"
          AfterTargets="Build">
    <PropertyGroup>
      <AfterBuildRan>true</AfterBuildRan>
    </PropertyGroup>
  </Target>
</Project>
