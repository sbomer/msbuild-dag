<Project>
  <PropertyGroup>
    <Subset>clr+libs-native</Subset>
  </PropertyGroup>

  <Target Name="Build">
    <ItemGroup>
      <SpecifiedSubsetName Include="$([MSBuild]::Unescape($(Subset.Replace('+', ';').Replace('-', ';'))))" />
    </ItemGroup>
  </Target>
</Project>
