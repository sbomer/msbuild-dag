<Project>
  <ItemGroup>
    <Action Include="Restore" />
    <Action Include="Build" />
  </ItemGroup>

  <Target Name="Build">
    <PropertyGroup>
      <RemoveProps>Projects;@(_Missing);@(Action);Publish</RemoveProps>
    </PropertyGroup>
  </Target>
</Project>
